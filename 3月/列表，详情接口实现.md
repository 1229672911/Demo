# 列表，详情接口实现

## 列表接口

**获取所有操作日志的简要信息，支持搜索、分页、排序和筛选**



**输入**：根据用户的需求进行搜索筛选，查看分页，排序和关键词筛选

**输出**：返回操作日志的列表，每条只包含关键信息



**1.接收请求参数**

- 从请求中获取分页参数
- 获取筛选条件

**2.查询数据库**

- 使用$GORM$构建查询语句，根据条件过滤数据，并按照排序参数排序
- 使用$GORM$的$Offset$和$Limit$实现分页

**3.返回结果**

 - 将查询到的日志封装成一个结构体，然后以$JSON$的形式返回给客户端





**在计算第几页的时候**

```go
PageInt, _ := strconv.Atoi(Page)//自动数据类型转换，比如将字符串转换成int
```



**几个特定的返回值的作用**

```go
query = query.Offset(offset).Limit(PageSizeInt).Order(OrderBy) //在第几页，每页多少条，以及排序方式
```

$Offset()$：偏移量，表示跳过前offset条记录

$Limit()$：设置查询的返回记录量，即每页显示多少条记录

$Order()$：设置查询的排序规则，可以是单个字段，也可以是带排序方式的字段



**$DefaultQuery()$语法**

```go
func (c *Context) DefaultQuery(key, defaultValue string) string
```

**参数**

- $key$：要获取的查询参数的键名。
- $defaultValue$：如果查询参数不存在或为空，返回的默认值。

**返回值**

- 返回查询参数的值（$string$ 类型）。如果参数不存在或为空，返回 $defaultValue$。



**$Query()$语法**

```go
func (c *Context) Query(key string) string
```

**参数**

- $key$：要获取的查询参数的键名。

**返回值**

- 返回查询参数的值（$string$ 类型）。如果参数不存在或为空，返回空字符串 `""`。



**$Where()$用法**

**语法**

```go
query = query.Where("条件", 参数1, 参数2, ...)
query = query.Where("name = ? AND age >= ?", "Alice", 20)
```

**参数**

- **条件**：$SQL$ 的 $Where$ 子句，可以包含占位符 `?`。
- **参数**：替换占位符的变量（可以是字段值或其他查询条件）。

**返回值**

- 返回一个新的查询对象，支持链式调用,可以与其他方法（如 $Limit$、$Offset$）组合使用。



**$Order()$用法**

**语法**

```go
query = query.Order("排序规则")
```

**参数**

- 排序规则

  指定排序字段和排序方式。

  - 单个字段：$field\ ASC$ 或 $field\  DESC$。
  - 多个字段：$field1\ ASC, field2\ DESC$。
  - $ASC$升序,$DESC$降序

**返回值**

- 返回一个新的查询对象，支持链式调用。



**$query := db.Model()$**

- 用于指定要操作的数据库表

- 返回一个新的查询对象，后续可以链式调用其他方法

相当于之后都是对这个查询对象操作



**了解$JSON$的作用和格式，扮演着什么样的角色**

- $JSON$ 是一种轻量级的 **数据交换格式**，全称是 **JavaScript Object Notation**。它基于文本，易于人阅读和编写，同时也易于机器解析和生成。

- $JSON $数据是由 **键值对** 组成的，用 `{}` 表示对象，用 `[]` 表示数组。

```go
{
  "name": "Alice",
  "age": 25,
  "isStudent": false,
  "hobbies": ["reading", "coding", "traveling"],
  "address": {
    "city": "Beijing",
    "country": "China"
  }
}

```



如果要根据多个条件进行查询





## 详情接口

**查看列表里所有的详情信息**



**输入**：用户通过请求传递日志$ID$

**输出**：返回该操作的详情信息（包括所有字段）



**1.接收请求参数**

- 从请求中获取操作日志的$ID$

**2.查询数据库**

- 使用$GORM$ 查询对应的日志记录。

**3.返回结果**

- 将查询到的日志详细信息封装成一个结构体，然后以 $JSON$ 格式返回给客户端。



**$Param()$用法**

**作用**

$Param()$是 $Gin $框架中用于获取$ URL$ 参数的方法。它从请求的$ URL $路径中提取参数值。

**语法**

```
func (c *Context) Param(key string) string
```

**参数**

- $key$：$URL$ 路径中定义的参数名。

**返回值**

- 返回 $URL$ 参数的值（字符串类型）。如果参数不存在，则返回空字符串。



**$First()$用法**

**作用**

$First()$ 是 $GORM$ 中用于查询数据库第一条记录的方法。它根据条件查询数据库，并将结果映射到指定的结构体中。

**语法**

```
func (db *DB) First(dest interface{}, conds ...interface{}) *DB
```

**参数**

1. $dest$：目标结构体或 $Map$，用于存储查询结果。
2. $conds$：查询条件（可选）。

**返回值**

- 返回一个 $*DB$对象，支持链式调用。 $Deprecated$参数 如果未找到记录，$First()$ 会返回 $gorm.ErrRecordNotFound$错误。

