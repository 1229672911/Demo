# $Go$语言核心$36$讲笔记

## 工作区和$GOPATH$

**设置 $GOPATH$ 有什么意义**

- $GOPATH$可以看作$GO$语言的工作目录，他的值是一个目录的路径，也可以是多个目录的路径，每个目录都代表$GO$语言的一个工作区。这个工作区可以防止$GO$语言的源码文件以及安装后的归档文件和可执行文件。                            
- 其实在之前$GOPATH$时代必须不能放$go.mod$文件，且执行文件必须放在$GOPATH$目录下。

- 而在之后$Module$模式之后可以任意放置位置，但是必须使用$go.mod$文件，且$go.mod$文件必须明确记录依赖版本。

**有关的三个知识点有**

1. **$Go$语言源码的组织方式是怎样的**

- 以代码包为基本组织单位，每个目录就是一个包，同目录下的.go文件必须声明同一个包名。同时，由于目录可以有子目录，所以代码包也可以有子包。在文件系统中这些代码是与目录一一对应的。
- 一个代码包中可以包含任意个以$.go$为为扩展名的源码文件，这些源码文件都要被声明属于同一个代码包，代码包的名称一般会与源码文件所在的目录同名，如果不同名，那么就以代码包的名称为准。
- 每个代码包都有导入路径，代码包的导入路径是其他代码包在使用该包中的程序实体时，需要引入的路径。就是访问该包里的函数，结构体等东西的时候需要$import$这个包的路径。
- 在工作区中，一个代码包的导入路径实际上就是从$src$子目录到该包的相对路径。
- 所以说，$Go$语言源码的组织方式就是以环境变量$GOPATH$、工作区、$src$目录和代码包为主线的。一般情况下，$Go$语言源码文件都需要被存放在$GOPATH$包含的某个工作区（目录）中的$src$目录下的某个代码包（目录）中。但是可以通过导入相对路径的方式在其他地方调用。

2. **了解源码安装后的结果**

- 源码文件通常会被放在某个工作区的$src$子目录下。那么在安装后如果产生了归档文件（以.a为扩展名的文件），就会放进该工作区的$pkg$子目录。如果产生了可执行文件，就会放进该工作区的$bin$子目录。

- 源码文件会以代码包的形式组织起来，一个代码包就对应一个目录。安装某个代码包之后就会产生一个与他同名的归档文件。

- 归档文件就是把多个$.go$文件编译后打包成一个方便服用的二进制库文件。

- 归档文件的相对目录与 $pkg$ 目录之间还有一级目录，叫做平台相关目录。平台相关目录的名称是由 $build$（也称“构建”）的目标操作系统、下划线和目标计算架构的代号组成的。

  比如，构建某个代码包时的目标操作系统是 $Linux$，目标计算架构是 $64$ 位的，那么对应的平台相关目录就是 $linux\_amd64$。

  那么刚刚代码包的归档文件就会放置在当前工作区的子目录$pkg/linux\_amd64/xxx$之中

- 总之，某个工作区的$src$目录下的源码文件在安装后一般会被放置到当前工作区的$pkg$子目录下对应的目录中，或者被直接放置到该工作区的$bin$子目录中。

3. **理解构建和安装$Go$程序的过程**

- 构建使用的命令是$go\ build$，安装使用命令$go\ install$。构建和安装代码包的时候都会执行编译、打包的操作。并且，这些操作生成的任何文件都会先被保存到某个临时的目录中。
- 如果构建的是库源码文件，那么操作的结果文件只会存在于临时目录中。这里构建的主要意义在于检查和验证
- 如果构建的是命令源码文件，那么操作的结果文件会被搬运到源码文件所在的目录中。
- 安装操作会先执行构建，然后还会进行链接操作，并且把结果文件搬运到指定目录。进一步说，如果安装的是库源码文件，那么结果文件会被搬运到它所在工作区的$pkg$目录下的某个子目录中。
- 如果安装的是命令源码文件，那么结果文件会被搬运到它所在工作区的bin目录中，或者环境变量$GOBIN$指向的目录中。
- 想被复用，就写成库文件（非$main$包）。就像工具箱，提供各种函数给别人调用可以被$import$。包的声明可以是$main$外的任意包名，最后生成  $.a$归档文件。
- 想直接运行，就写成命令文件（$main$包+$main$函数）。就像遥控器，包含$main$函数，是程序入口。不能被$import$。包声明时必须声明为$package\ main$，最后生成可执行文件。





-----------

**？？Go 语言在多个工作区中查找依赖包的时候是以怎样的顺序进行的？？**

**？？如果在多个工作区中都存在导入路径相同的代码包会产生冲突吗？？**





















