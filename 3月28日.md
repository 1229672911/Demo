# 3月28日

## 昨天的一些疑问

--------

**？？ $type\ \ JsonResponse\ \ map[string]interface\{\}$ 关于其中$interface\{\}$的解释与延伸？？**

- 这里的$interface\{\} $相当于万能收纳箱，可以装任何类型的数据，但是取出时需要**类型断言**，就像从口袋掏出来东西需要确认是什么。

**？？那这里的$interface\{\} $和$gin.H$中的$any$有什么区别？？**

- 在$Go\ \ 1.18$之后，$interface\{\} $和 $any$ 是**完全等价的**，在编译后完全相同。就像「番茄」和「西红柿」的关系，只是别名不同。

**？？类型断言是什么？？**

- 类型断言是$Go$语言中**把接口值转换为具体类型**的机制，语法有两种形式：

```go
// 暴力版（错误会panic）
value := someInterface.(具体类型) 

// 安全版（推荐使用）
value, ok := someInterface.(具体类型)
```

```go
//如在处理JSON解析结果时
var data interface{} = "hello" // 接口里装着string

// 尝试取出string值
str, ok := data.(string)
if ok {
    fmt.Println("是字符串:", str) // 输出: hello
} else {
    fmt.Println("不是字符串！")
}
//注意，如果这里只用一个返回值，当interface{}里不是string则会返回一个错误。此时
```

---------------

## $SQL$查询（$SQLite$）

$SQLite$不区分大小写

注释是连续两个`--`字符，也可使用类似C++的以`/*`开始 ，以 `*/`结束









### 之前的那个接口在分页查询的时候还有一些问题

按照之前写的代码直接按照用户$id$查询的时候，会提示在$DTO$中的储存的分页相关的关键字不存在数据库表中。

### 修改$DTO$结构

```go
// web_model/log_data_dto.go
type LogDataDTO struct {
    // 分页参数（取消注释并修改类型）
    PageNumber int    `json:"pageNumber" form:"pageNumber" example:"1"`   // 第几页
    PageSize   int    `json:"pageSize" form:"pageSize" example:"10"`      // 每页条数
    OrderBy    string `json:"orderBy" form:"orderBy" example:"desc"`      // 排序方式
    
    // 原有查询条件
    OpLogId      string `json:"opLogId" form:"opLogId"`
    // ...其他字段保持不变
}
```



------------

**？？我要实现的是在一个页面有着多条数据为什么要将分页信息放在日志列表的$DTO$里面？会有影响吗？？**

- 理想情况下，分页参数属于“展示控制”，与业务查询条件本质不同。

- 而从现实考量

  ```mermaid
  graph LR
      A[前端请求] --> B[查询条件DTO]
      B --> C[Service层]
      C --> D[分页查询]
  
  ```

  如果单独传递分页参数，会导致方法参数膨胀或业务逻辑与分页逻辑割裂。

- 但是这样做理论上违反了$DTO$的“纯粹性”，如果未来分页逻辑变化，需要修改多个$DTO$。



---------------



之后用**嵌入分页基类**的方法来做会更好

```go
// nexus_log_getdata_dto.go
type Pagination struct {
    PageNumber int    `json:"pageNumber" form:"pageNumber" example:"1"`  // 使用小写开头的json标签
    PageSize   int    `json:"pageSize" form:"pageSize" example:"10"`
    OrderBy    string `json:"orderBy" form:"orderBy" example:"desc"`
}

type LogDataDTO struct {
    Pagination // 嵌入分页结构体
    
    // 原有业务字段（注意调整json标签为小写开头）
    OpLogId      string `json:"opLogId" example:"1"`          
    OpPeopleName string `json:"opPeopleName" example:"Alice"`
    // ...其他字段保持原样
}
```







### 增强Service层

```go
func QueryLogsByCondition(db *gorm.DB, dto web_model.LogDataDTO) ([]web_model.LogDataDTO, int64, error) {
    // 初始化分页参数
    if dto.PageNumber <= 0 {
        dto.PageNumber = 1
    }
    if dto.PageSize <= 0 {
        dto.PageSize = 10
    } else if dto.PageSize > 100 {
        dto.PageSize = 100
    }
    if dto.OrderBy == "" {
        dto.OrderBy = "desc"
    }

    // 构造基础查询
    query := db.Model(&sys_op_log_model.SysOpLog{})
    
    // 原有条件查询保持不变...
    if dto.OpLogId != "" {
        query = query.Where("op_log_id = ?", dto.OpLogId)
    }
    // ...其他条件

    // 先查询总数
    var total int64
    if err := query.Count(&total).Error; err != nil {
        return nil, 0, err
    }

    // 分页查询
    offset := (dto.PageNumber - 1) * dto.PageSize
    var logs []web_model.LogDataDTO
    err := query.
        Offset(offset).
        Limit(dto.PageSize).
        Order("created_at " + dto.OrderBy).
        Find(&logs).Error

    return logs, total, err
}

```



### 改造$Controller$       （$api\_log\_list.go$）

```go
func log(c *gin.Context, db *gorm.DB) {
    var dto web_model.LogDataDTO
    if err := c.ShouldBind(&dto); err != nil {
        c.JSON(http.StatusBadRequest, web_model.JsonResponse{
            "error": "参数解析失败: " + err.Error(),
        })
        return
    }

    // 调用Service（现在返回数据和总数）
    logs, total, err := service.QueryLogsByCondition(db, dto)
    if err != nil {
        c.JSON(http.StatusInternalServerError, web_model.JsonResponse{
            "error": "查询失败: " + err.Error(),
        })
        return
    }

    // 返回标准分页响应
    c.JSON(http.StatusOK, gin.H{
        "data":       logs,
        "total":      total,
        "pageNumber": dto.PageNumber,
        "pageSize":   dto.PageSize,
    })
}

```



**最后更新$Swagger$注释**



-----------------------

在第一次修改后终于能返回数据库的信息和分页查询信息了，但是分页查询信息返回的不对

$pageNumber$和$pageSize$都还是$0$

```json
{
    "data": [
        {
            "pageNumber": 0,
            "pageSize": 0,
            "orderBy": "",
            "OpLogId": "1001",
            "OpPeopleName": "张三",
            "IpAddress": "192.168.1.100",
            "ModuleName": "认证模块",
            "OpType": "用户登录",
            "RequestModel": "POST",
            "OpStatus": "成功"
        }
    ],
    "pageNumber": 0,
    "pageSize": 0,
    "total": 1
}
```

**问题**：

- 返回的$pageNumber$和$pageSize$都是$0$
- 但实际分页查询是有效的

**原因**：

- 在$LogDataDTO$中嵌入了$Pagination$结构体
- 当查询结果映射到$LogDataDTO$时，$GORM$会尝试填充这些字段
- 但数据库中没有这些字段，所以被设为零值







