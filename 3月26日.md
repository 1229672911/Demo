# 3月26日

## 容器

是一种**操作系统级别的虚拟化技术**，通过以下机制实现

- **命名空间：**对资源进行隔离，使得每个容器都拥有独立的进程、网络、文件系统等，确保容器之间互不干扰。
- **控制组：**限制和分配容器的资源（如$CPU$，内存，磁盘$I/O$等），确保每个容器的资源使用在合理范围内。
- **镜像：**容器的模板，包含应用程序及所有依赖。就像设计图纸，可以用来创建多个相同的容器。
- **运行时：**运行和管理的工具（如$containerd$或$runc$），负责启动、停止和管理容器。

----------------

**？？容器就是$Docker$吗？？**

--$Docker$是最流行的容器平台，但不是容器的唯一是实现。容器是一种技术概念，而$Docker$是使用这种技术的工具。

-------------

**容器的优点**

- **一致性：**无论在哪台机器上运行，容器的行为都是一致的。

- **轻量高效：**容器共享主机的操作系统内核，资源占用更少。

- **快速启动：**容器启动时间通常在秒级，而虚拟机需要几分钟。

- **易于部署：**容器可以快速部署和扩展，适合微服务架构。

## 闭包

就像一个随身携带的手册，保存了**函数上下文的环境（变量）**，即使在函数外部也能访问。

- 一个函数$A$返回了另一个函数$B$。
- 函数$B$可以访问函数$A$中的变量，即使函数$A$已经执行完毕。

**关键点**：

- **函数嵌套**：闭包通常涉及嵌套函数。

- **捕获变量**：内部函数可以访问外部函数的变量。

- **延长变量生命周期**：即使外部函数执行完毕，内部函数仍然可以访问其变量。

--------------

**？？相当于在执行完之后返回一个有自己详细信息的手册。是否有完整的读写权限，可以从$B$中修改$A$的变量？？**

--闭包不仅 **可以读取外部函数（$A$）的变量**，还 可以**修改这些变量**。这是因为闭包捕获的是变量的 **引用**，而不是变量的值。

- 外部函数的变量会被 **分配在堆上**，而不是栈上。
- 栈上的变量在函数执行完毕后会被销毁,堆上的变量在函数执行完毕后仍然存在。
- 外部函数的变量会被 **提升到堆上**，确保闭包可以访问和修改它们。



**？？函数的上下文环境具体是什么意思？？**

- **函数** 在执行时 **可以访问的所有变量和资源**。
- 上下文环境决定了函数 **可以访问哪些数据**，以及函数 **执行时的行为**。
  - 提供数据：体哦那个如局部变量，外部变量，和参数等。
  - 决定行为：上下文环境中的变量和资源会影响函数的**执行逻辑**
    - 例如：如果函数访问了一个数据库链接，函数的执行将依赖于该链接的状态。

----------

## 再写列表、详情接口

### 注册路由

```go
func ResignerLogList(router *gin.Engine, container *nexus_wire.AppContainer) {
	api := router.Group("/Log-list") //注册一个路由组

	api.GET("/log", func(c *gin.Context) {
		log(c, container.GetDB())
	})
	api.GET("/log/detail", func(c *gin.Context) {
		logDetail(c, container.GetDB())
	})

}
```



### 列表接口	$log$

#### 列表接口逻辑实现

- 首先获取请求参数，在第几页，每页多少个，按照什么类型排序和查找的限制信息。将这些查询条件写到$web\_modle$里的$dto$。通过$dto$对参数统一管理可以

- 查询列表并以$JSON$的形式返回数据，按照每页多少个，在第几页，以什么顺序返回。这个方法要写在$Service$里。

- 同时要实现的查询逻辑应该为**无关键词时忽略该字段**
  - 如果某个字段的关键词不为空，则将其加入查询条件。
  - 如果某个关键词的结果为空，则忽略该字段。

#### $api$实现

```go
func log(c *gin.Context, db *gorm.DB) { //查看列表
	//解析查询条件
	var dto web_model.LogDataDTO
	if err := c.ShouldBind(&dto); err != nil {
		c.JSON(http.StatusBadRequest, gin.H{"参数解析失败\n": err.Error()})
		return
	}
	//调用Services层查询
	logs, err := service.QueryLogsByCondition(db, dto)
	if err != nil {
		c.JSON(http.StatusInternalServerError, gin.H{"查询错误\n": err.Error()})
		return
	}
	c.JSON(http.StatusOK, gin.H{
		"data": logs,
	})
}
```

- $ShouldBind(\&dto):$是$Gin$提供的一个方法，用于将请求中的数据绑定到一个结构体中，而$\&dto$可以将$dto$变量的地址传递给$ShouldBind$使得其可以直接修改$dto$

--------------------

**？？是否可以理解为$ShouldBind$将所接受到的数据转换成$dto$这个结构体格式的数据之后再将值赋给他？？**

- 可以，$ShouldBind$ 的核心功能就是 **将接收到的数据转换成$DTO$结构体格式的数据，并将这些值赋给$DTO$ 的字段**

------------------



#### $services$实现

```go
func QueryLogsByCondition(db *gorm.DB, dto web_model.LogDataDTO) ([]web_model.LogDataDTO, error) {
	var logs []web_model.LogDataDTO
	// 构造查询语句
	query := db.Model(&sys_op_log_model.SysOpLog{})
	// 如果某个字段的关键词不为空，则加入查询条件
	if dto.OpLogId != "" {
		query = query.Where("op_log_id = ?", dto.OpLogId)
	}
	if dto.OpPeopleName != "" {
		query = query.Where("op_people_name LIKE ?", "%"+dto.OpPeopleName+"%")
	}
	if dto.IpAddress != "" {
		query = query.Where("ip_address = ?", dto.IpAddress)
	}
	if dto.ModuleName != "" {
		query = query.Where("module_name LIKE ?", "%"+dto.ModuleName+"%")
	}
	if dto.OpType != "" {
		query = query.Where("op_type = ?", dto.OpType)
	}
	if dto.RequestModel != "" {
		query = query.Where("request_model = ?", dto.RequestModel)
	}
	if dto.OpStatus != "" {
		query = query.Where("op_status = ?", dto.OpStatus)
	}
	// 分页查询
	if dto.PageNumber != "" && dto.PageSize != "" {
		pageNumber, _ := strconv.Atoi(dto.PageNumber)
		pageSize, _ := strconv.Atoi(dto.PageSize)
		offset := (pageNumber - 1) * pageSize
		query = query.Offset(offset).Limit(pageSize)
	} else {
		query = query.Offset(0).Limit(10)
	}
	// 排序
	if dto.OrderBy != "" {
		query = query.Order("created_at " + dto.OrderBy)
	}
	// 执行查询
	if err := query.Find(&logs).Error; err != nil {
		return nil, err
	}
	return logs, nil

}
```





### 详情接口	$logDetail$

#### 详情接口逻辑实现

- 请求参数是确定的，如日之编号$ID$
- 查询所有详情信息并以$JSON$的形式返回，写到$Services$里

#### $api$层实现

```go
// 查看列表详情
func logDetail(c *gin.Context, db *gorm.DB) { //查看列表详情
	//解析查询条件
	var dto web_model.LogDataDetailDTO
	if err := c.ShouldBind(&dto); err != nil {
		c.JSON(http.StatusBadRequest, gin.H{"参数解析失败\n": err.Error()})
		return
	}
	// 调用 Service 层查询
	logDetail, err := service.QueryLogDetailByOpLogId(db, dto.OpLogId)
	if err != nil {
		c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
		return
	}
	// 返回结果
	c.JSON(http.StatusOK, gin.H{
		"data": logDetail,
	})

}
```

#### $services$层实现

```go
// 列表详情查看
func QueryLogDetailByOpLogId(db *gorm.DB, opLogId string) (*web_model.LogDataDetailDTO, error) {
	var logDetail web_model.LogDataDetailDTO

	// 构造查询语句
	query := db.Model(&sys_op_log_model.SysOpLog{})

	// 根据 OpLogId 查询日志详情
	query = query.Where("op_log_id = ?", opLogId)

	// 执行查询
	if err := query.First(&logDetail).Error; err != nil {
		return nil, err
	}

	return &logDetail, nil
}
```





$api$用来决定要调用那些方法，这些方法在$services$层中实现，如果有参数需要传的话就写到$DTO$层里再调用